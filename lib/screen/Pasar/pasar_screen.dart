import 'package:flutter/material.dart';

class PasarScreen extends StatelessWidget {
  const PasarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
